{
  "//Urls": "Fallback binding used whenever launchSettings.json is NOT in play (dotnet run --no-launch-profile, or running the built DLL). Kept equal to the launchSettings http port so the API answers on 5178 either way. Lives here rather than in appsettings.Development.json because that file is gitignored and this value must survive a fresh clone. See plan.md section 2.1.",
  "Urls": "http://localhost:5178",
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=TaskManagementDb;Trusted_Connection=True;TrustServerCertificate=True"
  },
  "Cors": {
    "AngularOrigin": "http://localhost:4200"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
